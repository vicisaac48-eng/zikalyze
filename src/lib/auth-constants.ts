// Auth storage keys - shared between main.tsx (pre-render check) and useAuth.ts
// Keeping these in a separate file ensures consistency across the codebase

// Key used to store wallet data in localStorage
export const WALLET_DATA_KEY = "zikalyze_wallet_data";
